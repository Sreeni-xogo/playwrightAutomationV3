# 3. EnvAndTsConfig

**Goal**: Set up `.env.example`, `tsconfig.json`, and install `@playwright/cli`
**Est.**: ≤1 hour
**Dependencies**: 01.FolderStructure

## Steps
- [ ] Create `.env.example` with all required vars (URLs + credentials keys)
- [ ] Create `.env` with actual values (gitignored)
- [ ] Verify `.gitignore` includes `.env`
- [ ] Create `tsconfig.json` — strict mode, ES modules, path aliases for `e2e/`
- [ ] Install `@playwright/cli` — `npm install --save-dev @playwright/cli`

## Definition of Done
- [ ] `.env.example` committed, `.env` gitignored
- [ ] `tsconfig.json` valid — `tsc --noEmit` passes
- [ ] `@playwright/cli` present in `package.json` devDependencies

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Follow-ups**:
