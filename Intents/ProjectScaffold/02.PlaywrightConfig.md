# 2. PlaywrightConfig

**Goal**: Configure `playwright.config.ts` with 4 named environment projects, Chromium only, env var base URLs
**Est.**: ≤1 hour
**Dependencies**: 01.FolderStructure

## Steps
- [ ] Replace generated `playwright.config.ts` with project-specific config
- [ ] Define 4 projects: `local`, `staging`, `pre-release`, `production`
- [ ] Each project reads base URL from env var (`LOCAL_URL`, `STAGING_URL`, etc.)
- [ ] Set `testDir` to `e2e/`
- [ ] Configure `reporter`, `trace`, `screenshot`, `video` settings
- [ ] Chromium only — single browser project per env

## Definition of Done
- [ ] `npx playwright test --project=staging` resolves correct base URL
- [ ] Config is valid TypeScript with no errors

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Follow-ups**:
