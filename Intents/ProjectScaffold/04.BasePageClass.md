# 4. BasePageClass

**Goal**: Create `BasePage` POM class with shared methods that all page objects will extend
**Est.**: ≤1 hour
**Dependencies**: 01.FolderStructure, 03.EnvAndTsConfig

## Steps
- [ ] Create `e2e/pages/BasePage.ts`
- [ ] Constructor accepts `Page` from Playwright
- [ ] Add `navigate(path: string)` method
- [ ] Add `waitForLoad()` method — waits for `networkidle` or `domcontentloaded`
- [ ] Add `getTitle()` method — returns page title
- [ ] Add `getUrl()` method — returns current URL
- [ ] Export class using named export, single quotes throughout

## Definition of Done
- [ ] `BasePage.ts` created with no TypeScript errors
- [ ] All methods use single quotes
- [ ] No regex used anywhere in the file

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Follow-ups**:
