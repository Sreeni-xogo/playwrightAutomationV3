# 03. DashboardAndLibraryWaits

**Goal**: Add load guards to DashboardPage, LibraryPage, UploadPage.
**Est.**: ≤1.5 hours
**Dependencies**: Intent 01 (UpgradeBasePage)

## Steps
- [ ] DashboardPage: update `goto()` to use `waitForLoadAndElement(this.pageHeading)`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavLibrary()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavPlaylists()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavPlayers()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavPlanner()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavOverlays()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavWidgets()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickNavTeams()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickLibraryViewAll()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickPlaylistsViewAll()`
- [ ] DashboardPage: add `waitForLoad()` after click in `clickPlayersViewAll()`
- [ ] LibraryPage: update `goto()` to use `waitForLoadAndElement(this.pageHeading)`
- [ ] LibraryPage: add `waitForLoad()` after click in `clickFirstItem()`
- [ ] LibraryPage: add `waitForLoad()` after click in `clickAddNewMedia()`
- [ ] LibraryPage: add `waitForLoad()` after click in `clickAddNewUrl()`
- [ ] LibraryPage: add `waitForLoad()` after click in `clickAddNewWidget()`
- [ ] LibraryPage: add `waitForLoad()` after click in `goToNextPage()`
- [ ] LibraryPage: add `waitForLoad()` after click in `goToPrevPage()`
- [ ] LibraryPage: add `waitForLoad()` after click in `goToFirstPage()`
- [ ] LibraryPage: add `waitForLoad()` after click in `goToLastPage()`
- [ ] LibraryPage: add `waitForLoad()` after click in `goToPage()`
- [ ] UploadPage: update `goto()` to use `waitForLoadAndElement` on primary element (if goto exists)

## Definition of Done
- [ ] All 3 POM files updated
- [ ] Every `goto()` uses `waitForLoadAndElement`
- [ ] Every click-nav method has `waitForLoad()` after click
- [ ] No spec files modified

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Key Decisions**:
- **Follow-ups**:
