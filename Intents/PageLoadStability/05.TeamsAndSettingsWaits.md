# 05. TeamsAndSettingsWaits

**Goal**: Add load guards to Teams POMs (3 files) and all Settings POMs (10 files).
**Est.**: ≤2 hours
**Dependencies**: Intent 01 (UpgradeBasePage)

## Steps
- [ ] TeamsPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] TeamDetailPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] TeamsGroupingPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] ProfilePage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] MembersPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] LicensesPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] BillingPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] BillingPlansPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] ReferralPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] HandoverDeletePage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] IntegrationPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] ServicesPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] CompanyPlayersPage: update `goto()` if present; add `waitForLoad()` to all nav methods

## Definition of Done
- [ ] All 13 POM files updated
- [ ] Every `goto()` uses `waitForLoadAndElement` on primary heading/element
- [ ] Every click-based navigation method has `waitForLoad()` after click
- [ ] No spec files modified

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Key Decisions**:
- **Follow-ups**:
