# 02. AuthPageWaits

**Goal**: Add load guards to all 3 auth POMs.
**Est.**: ≤1 hour
**Dependencies**: Intent 01 (UpgradeBasePage)

## Steps
- [ ] SignInPage: update `goto()` to use `waitForLoadAndElement(this.logoElmt)`
- [ ] SignInPage: add `waitForLoad()` after `clickLoginButton()` inside `login()`
- [ ] SignInPage: add `waitForLoad()` after click in `navigateToForgotPassword()`
- [ ] SignInPage: add `waitForLoad()` after click in `navigateToSignUp()`
- [ ] SignUpPage: update `goto()` to use `waitForLoadAndElement(this.heading)`
- [ ] SignUpPage: add `waitForLoad()` after `nextButton.click()` in `clickNext()`
- [ ] SignUpPage: add `waitForLoad()` after `backButton.click()` in `clickBack()`
- [ ] SignUpPage: add `waitForLoad()` after click in `navigateToLogin()`
- [ ] ForgotPasswordPage: update `goto()` to use `waitForLoadAndElement(this.heading)`
- [ ] ForgotPasswordPage: add `waitForLoad()` after click in `navigateToLogin()`
- [ ] ForgotPasswordPage: add `waitForLoad()` after `clickResetPassword()` in `submitResetRequest()`

## Definition of Done
- [ ] All 3 auth POM files updated
- [ ] Every `goto()` calls `waitForLoadAndElement` on a primary visible element
- [ ] Every click-based navigation method has `waitForLoad()` after the click
- [ ] No spec files modified

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Key Decisions**:
- **Follow-ups**:
