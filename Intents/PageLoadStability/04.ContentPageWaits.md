# 04. ContentPageWaits

**Goal**: Add load guards to Playlists, Players, Planners, Overlays, Widgets POMs (10 files).
**Est.**: ≤2 hours
**Dependencies**: Intent 01 (UpgradeBasePage)

## Steps
- [ ] PlaylistsPage: update `goto()`; add `waitForLoad()` to `clickAddNew()`, `clickPlaylist()`
- [ ] PlaylistEditPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] PlayersPage: update `goto()`; add `waitForLoad()` to `clickAddNew()`, `clickPlayer()`
- [ ] PlayerDetailPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] PlannersPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] PlannerEditPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] OverlaysPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] OverlayEditPage: update `goto()` if present; add `waitForLoad()` to all nav methods
- [ ] WidgetsPage: update `goto()`; add `waitForLoad()` to all nav/click methods
- [ ] WidgetEditPage: update `goto()` if present; add `waitForLoad()` to all nav methods

## Definition of Done
- [ ] All 10 POM files updated
- [ ] Every `goto()` uses `waitForLoadAndElement` on primary heading/element
- [ ] Every click-based navigation method has `waitForLoad()` after click
- [ ] No spec files modified

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**:
- **Key Decisions**:
- **Follow-ups**:
